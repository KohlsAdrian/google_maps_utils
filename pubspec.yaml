name: google_maps_utils
description: 
  Initial version comes with the 3 main Classes. SphericalUtils, MathUtils and PolyUtils, calculating bounds, distances headings and many more.

homepage: https://github.com/KohlsAdrian/google_maps_utils/
version: 1.1.0+3

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  stack: ^0.1.0

dev_dependencies:
  #flutter_test:
  #  sdk: flutter

flutter:

  uses-material-design: true